# Instant Quote

This repository now includes a small animated to-do list application built with Python and the [Rich](https://pypi.org/project/rich/) library.

## Running

```bash
pip install rich
python todolist.py
```

Use the password `root123` to unlock the application. Tasks are stored in `tasks.json` and ignored by git.
